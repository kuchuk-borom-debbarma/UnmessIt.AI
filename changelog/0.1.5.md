## 0.1.5

- **Fix**: Allowed documentation-only PRs to bypass the version bump requirement in the staging release gate.
- **Fix (Web)**: Resolved a UI bug where the "expand/collapse" button on very long notes failed to render on initial page load due to clipped height measurements.
- **Build**: Dramatically improved Docker and GitHub Actions CI build times (especially for multi-arch/QEMU) by fixing layer caching, adding proper BuildKit cache mounts (`--mount=type=cache`), and removing redundant `uv` syncs.
- **Docs**: Massively overhauled the documentation structure in `server/docs/`, deleting obsolete documents and creating the definitive `ARCHITECTURE_DEEP_DIVE.md` engineering specification.
- **Docs**: Rewrote the `README.md` and `current-state.md` to cleanly separate user-facing features from internal system architecture (EDA, RAG, Idempotency).
